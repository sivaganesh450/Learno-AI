# Web framework
fastapi==0.115.0
python-multipart==0.0.12
httpx>=0.27.0

# AWS Lambda adapter
mangum==0.17.0

# AWS SDK (boto3 is in Lambda runtime, but langchain-aws needs a recent version)
boto3>=1.34.72

# Database
pymongo==4.6.3
motor==3.3.2
dnspython
certifi

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.1

# Pydantic
pydantic[email]>=2.9.2
pydantic-settings>=2.5.2

# LangChain ecosystem (all 0.2.x for langchain-core >=0.2 compatibility)
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-aws>=0.2.0
langgraph>=0.2.0

# Web search
tavily-python==0.7.21

# Document parsing
pypdf>=4.0.0
python-docx>=1.0.0

# Image processing
pillow>=10.0.0

# Math / numeric
sympy>=1.12
numpy>=1.24.0

# Local development only (not needed in Lambda)
# uvicorn[standard]==0.32.0
# chromadb==1.4.1
